# -*- mode: ruby -*-
# vi: set ft=ruby :

# Vagrantfile API/syntax version. Don't touch unless you know what you're doing!
VAGRANTFILE_API_VERSION = "2"

Vagrant.configure(VAGRANTFILE_API_VERSION) do |config|
  config.vm.box = "chef/centos-6.5"
  config.vm.network "forwarded_port", guest: 8080, host: 8080
  config.vm.network "forwarded_port", guest: 5432, host: 5432
#  config.vm.network "private_network", ip: "192.168.0.35"
#  config.vm.network "private_network", ip: "10.0.0.100", virtualbox__intnet: "true"
  config.vm.network "private_network", ip: "10.0.0.100"
  config.vm.synced_folder ".", "/vagrant", type: "nfs"
  config.vm.provision :shell, path: "bootstrap.sh"
  config.vm.provider "virtualbox" do |v|
    v.memory = 1024
  end
end



#  config.vm.synced_folder "/vagrant", "/Users/Gardner/projetosweb", :mount_options => ["dmode=777,fmode=777"]
#  config.vm.network "private_network", ip: "192.168.0.169"
